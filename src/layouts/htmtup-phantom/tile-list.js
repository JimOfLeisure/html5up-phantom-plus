import React from 'react';

// TODO : Make this data-based and using a Tile component
const TileList = ({ children }) => (
  <section className="tiles">{children}</section>
);

export default TileList;
